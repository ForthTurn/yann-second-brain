---
import ArticleLayout from "../../layouts/ArticleLayout.astro";
import { Image } from "astro:assets";
import cardsAndCabinets from "../../assets/notes/seven-hash-collisions/cards-and-cabinets.png";

const article = {
  title: "七种哈希同时碰撞，能严格证明它存在吗？",
  description: "从一张修改 PDF 的梗图出发，用鸽巢原理证明共同碰撞必然存在，并分清存在性、搜索难度与第二原像问题。",
  publishedAt: "2026-09-20",
  tags: ["密码学", "数学", "哈希"],
  toc: [
    { href: "#question", label: "从梗图到数学命题" },
    { href: "#my-understanding", label: "我的理解：只看输入与输出" },
    { href: "#proof", label: "233 字节的存在性证明" },
    { href: "#filter", label: "筛选为何不会破坏条件" },
    { href: "#difficulty", label: "存在不等于找得到" },
    { href: "#boundaries", label: "这没有证明什么" },
    { href: "#plain-language", label: "说人话：柜子总会装重" },
    { href: "#sources", label: "参考资料" },
  ],
};
const stages = [
  ["起始集合", "—", 1864],
  ["MD5", "128", 1736],
  ["CRC32", "32", 1704],
  ["SHA-1", "160", 1544],
  ["SHA-256", "256", 1288],
  ["SHA-512", "512", 776],
  ["SHA3-256", "256", 520],
  ["SHA3-512", "512", 8],
];
---

<ArticleLayout {...article}>
  <p class="lead">这次讨论起于下面这张梗图：有人愿意出 4000 元，寻找修改 PDF 却保持七种哈希或校验值全部不变的方法，或者找出七项结果都相同的不同文件。</p>
  <figure class="meme-figure">
    <a href="/images/notes/seven-hash-collisions/pdf-hash-request.jpg" aria-label="查看七种哈希校验需求的完整原图">
      <img src="/images/notes/seven-hash-collisions/pdf-hash-request.jpg" width="1280" height="1682" alt="引发讨论的聊天截图：提出修改 PDF 或寻找不同文件的需求，要求 MD5、CRC32、SHA-1、SHA-256、SHA-512、SHA3-256、SHA3-512 七项结果同时不变，并报价 4000 元。" decoding="async" />
    </a>
    <figcaption>聊天截图：有人报价 4000 元，要求修改 PDF 后七种哈希或校验值全部不变，或找到七项结果都相同的不同文件。</figcaption>
  </figure>
  <p>看到这张图后，我更好奇的是一个数学问题：先不考虑怎么找到样本，能否严格证明，存在两个不同输入，能让这七项结果同时相同？于是我和 GPT 从这个问题出发，一步步讨论了输出组合的计数、鸽巢原理，以及“存在”和“找得到”之间的区别。</p>
  <p>下面整理的是这次讨论的证明与边界。原图混合了“修改指定文件”和“自由寻找两个文件”两种要求；我们先证明后者在原始字节串上的存在性，再回头看它为什么没有直接解决原图中的 PDF 修改需求。</p>
  <aside class="callout">
    <strong>可以，但要说清楚证明对象。</strong>
    <p>在全部恰好 233 字节的原始字节串中，必然存在至少 256 个不同输入，它们的七项结果分别完全相同。这是无条件的计数结论；它既没有提供可行的搜索方法，也没有证明某份指定 PDF 能按要求修改。</p>
  </aside>

  <section id="question">
    <h2>先把需求写成准确的命题</h2>
    <p>这里的七项是 MD5、CRC32、SHA-1、SHA-256、SHA-512、SHA3-256 和 SHA3-512。其中 CRC32 是校验码，不是密码学哈希。以下固定各算法的版本、参数和计算规则，全部对同一份完整字节串计算。</p>
    <p>把它们记作 H₁ 到 H₇。我们要找的是<strong>同一对</strong>不同输入 A、B，满足：</p>
    <div class="formula" aria-label="存在不同的 A 和 B，使每个 i 从一到七都有 H i 的 A 值等于 B 值">∃ A ≠ B，∀ i ∈ &#123;1, …, 7&#125;，Hᵢ(A) = Hᵢ(B)</div>
    <p>这不是找七对文件，每对只满足一项；也不是要求 MD5(A) 等于 SHA-256(A)。比较始终发生在<strong>同一个算法、两个不同输入</strong>之间。</p>
    <p>定义一个七元组，把每个输入的全部结果完整记录下来：</p>
    <div class="formula">F(x) = (H₁(x), H₂(x), …, H₇(x))</div>
    <p>于是 F(A) = F(B) 当且仅当七个等式全部成立。把结果写成元组，或按固定长度拼接二进制摘要，只是记法变化，并没有减少条件，也没有赋予我们单独操控各项结果的能力。</p>
  </section>

  <section id="my-understanding">
    <h2>我的理解：只看输入与输出，就已经够了</h2>
    <p>我觉得更简单直接的理解是：要证明七种哈希同时碰撞的情况存在，不必先研究算法之间会不会“互相冲突”。那样容易把存在性问题变成如何逐项构造碰撞的问题，反而复杂化了。直接从输入和最终输出出发：<strong>只要不同输入的数量大于输出组合的数量，根据抽屉原理（也叫鸽巢原理），就一定有不同输入得到完全相同的七项结果。</strong></p>
    <p>于是，关键就变成了：七项结果一共能表示多少种组合？这里有一个很有用的观察：<strong>七个固定长度输出的有序组合，与把这七段 bit 串按固定顺序拼接起来，是完全等价的表示。</strong>因为每段长度已知，拼接后仍能按固定边界拆回原来的七项，没有丢失任何信息。因此，拼接结果相同，当且仅当七项结果分别相同。</p>
    <div class="formula" aria-label="七项输出的有序组合，等价于按固定顺序拼接七段二进制串">(H₁(x), …, H₇(x)) ↔ H₁(x) ‖ … ‖ H₇(x)</div>
    <p>这里的 ‖ 表示拼接。七段加起来是 1856 位，因此这种表示一共能容纳 2<sup>1856</sup> 种组合。实际算法未必能产生其中每一种，但我们只需要这个上界：<strong>输出最多有这么多种，就已经足够证明碰撞存在。</strong></p>
    <p>如果把输入抽象为任意长度的有限字节串，输入的可能性是无限的，而固定长度输出的可能性有限，碰撞自然不可避免。具体哈希标准可能限制消息长度，不过这个思路甚至不需要用到无限：只看全部恰好 233 字节的输入，就有 2<sup>1864</sup> 种，已经超过输出的容量 2<sup>1856</sup>。</p>
    <p>更一般地，对算法共同支持的任意固定长度 L，只要 L ≥ 233 字节，全部该长度的原始字节串中就必然存在共同碰撞。这说的是<strong>整个候选集合中一定有碰撞</strong>，并不表示每个达到这个长度的文件都有碰撞对象，也不表示 233 字节是实际碰撞的最短长度。</p>
    <p>对我来说，这个证明最关键的转变就是：不用追踪算法内部怎样计算，只要看清输入有多少种、最终输出最多能容纳多少种，结论就出来了。</p>
  </section>

  <section id="proof">
    <h2>不需要无限输入，233 字节就够了</h2>
    <p>七项输出的长度分别为 128、32、160、256、512、256、512 位，总和是：</p>
    <div class="formula">N = 128 + 32 + 160 + 256 + 512 + 256 + 512 = 1856 位</div>
    <p>位数依据来自 <a href="https://www.rfc-editor.org/rfc/rfc1321">MD5 规范</a>、<a href="https://www.rfc-editor.org/rfc/rfc1952">CRC32 的规范示例</a>、<a href="https://csrc.nist.gov/pubs/fips/180-4/upd1/final">FIPS 180-4</a> 和 <a href="https://csrc.nist.gov/pubs/fips/202/final">FIPS 202</a>。这里数的是二进制位，不是十六进制显示的字符数。</p>
    <p>所以 F 的输出组合<strong>最多</strong>只有 2<sup>1856</sup> 种。这里不要求七种函数独立，也不要求每种组合都能出现；相关性或不可达的组合只会让实际输出种类更少。</p>
    <p>再令 S 为所有恰好 233 字节的输入。每字节有 256 种取值，因此：</p>
    <div class="formula">|S| = 256<sup>233</sup> = 2<sup>1864</sup> &gt; 2<sup>1856</sup></div>
    <figure class="count-diagram">
      <div><strong>2<sup>1864</sup></strong><span>不同的 233 字节输入</span></div>
      <span class="diagram-arrow" aria-hidden="true">→</span>
      <div><strong>至多 2<sup>1856</sup></strong><span>七项结果的组合</span></div>
      <figcaption>输入比可容纳它们的结果类别更多，至少一个类别必须容纳多个不同输入。</figcaption>
    </figure>
    <p><strong>鸽巢原理：</strong>把 M 个对象放进至多 K 个盒子，至少有一个盒子装着不少于 ⌈M/K⌉ 个对象。否则，每个盒子的数量都小于 M/K，加起来就不足 M，与总数矛盾。</p>
    <p>按 F(x) 的值把 S 分组，每组就是一个“盒子”。至少有一组的大小不小于：</p>
    <div class="formula">2<sup>1864</sup> / 2<sup>1856</sup> = 2<sup>8</sup> = 256</div>
    <p>这一组中的输入彼此不同，但 F 值全部相同。任取其中两个，就是同时满足七项相等的共同碰撞。<strong>证毕。</strong></p>
    <h3>为什么恰好选 233？</h3>
    <p>1856 位等于 232 字节。若固定输入长度为 L 字节，要让上述计数直接保证碰撞，只需 8L &gt; 1856，即 L &gt; 232，最小整数是 233。</p>
    <p>这是<strong>这套固定长度计数证明的充分门槛</strong>，不是实际碰撞的最短长度。对 232 字节或更短的输入，上述数量上界不足以保证碰撞，但也不能排除碰撞。证明使用有限、很短的输入，还避开了具体哈希标准对最大消息长度的限制。</p>
  </section>

  <section id="filter">
    <h2>“改第二项，会不会破坏第一项？”</h2>
    <p>这是讨论中最容易卡住的地方：为满足另一项而修改文件，确实可能破坏已经满足的条件。但存在性证明根本没有修改文件。它是在一批不变的候选中，反复选取子集。</p>
    <p>令 S₀ = S。第 i 步只在 Sᵢ₋₁ 内按 Hᵢ 分组，取最大的组 Sᵢ。若该算法输出 nᵢ 位，那么最多分出 2<sup>nᵢ</sup> 组，故：</p>
    <div class="formula">Sᵢ ⊆ Sᵢ₋₁，|Sᵢ| ≥ |Sᵢ₋₁| / 2<sup>nᵢ</sup></div>
    <div class="table-scroll" role="region" aria-label="逐步筛选的数量下界" tabindex="0">
      <table>
        <caption>每一步保留此前所有相等条件，再增加一项</caption>
        <thead><tr><th scope="col">本步新增条件</th><th scope="col">输出位数</th><th scope="col">保留输入至少为</th></tr></thead>
        <tbody>{stages.map(([name, bits, exponent]) => <tr><th scope="row">{name}</th><td>{bits}</td><td>2<sup>{exponent}</sup>{exponent === 8 && " = 256"}</td></tr>)}</tbody>
      </table>
    </div>
    <p>因为 S₇ ⊆ S₆ ⊆ … ⊆ S₁，最后留下的每个输入都在此前选出的各组里，因而继承所有相等条件。最后仍至少剩下 256 个输入。这个证明与元组计数是同一件事的两种表达。</p>
    <p><strong>乘起来的是分组数量的上界，不是独立事件的概率。</strong>整个论证没有随机性或独立性假设；它也不意味着实际枚举这些集合是可行的。</p>
  </section>

  <section id="difficulty">
    <h2>存在性证明，不是搜索难度证明</h2>
    <p>“在有限时间内不可能找到”并不准确。这里的候选集合是有限的，每次哈希计算也会结束。理论上，依次枚举不同的 233 字节输入并记录七元组，至多计算 2<sup>1856</sup> + 1 个候选就必然发现重复。这是有限但极其庞大的穷举上界，不是必要计算次数的下界。</p>
    <h3>2<sup>928</sup> 从哪里来？</h3>
    <p>若额外假设 F 像一个均匀随机的 1856 位函数，不同输入的结果独立，那么在 q 个不同候选中，碰撞对数的期望为 q(q − 1) / (2 × 2<sup>1856</sup>)。达到常数级碰撞概率所需的候选数因而约为：</p>
    <div class="formula">q ≈ 2<sup>1856 / 2</sup> = 2<sup>928</sup></div>
    <p>这是<strong>理想随机函数模型下的生日估算</strong>，不能当作这七个真实算法组合的实际安全强度，更不能声称所有算法都必须执行这么多次计算。</p>
    <p>一个简单反例：把同一个 SHA-256 摘要重复七遍，记录长度变成 1792 位，但一次 SHA-256 碰撞就让七段全部相等。输出更长，不自动意味着安全性同比增加。</p>
    <p>即便采用不同函数，也要研究结构。<a href="https://www.iacr.org/archive/crypto2004/31520306/multicollisions.pdf">Joux 的多重碰撞研究</a>说明，对某些迭代式哈希的拼接，结构性攻击可以优于把总输出当随机函数的朴素估算。它不是这七项组合已有实用攻击的证明，而是提醒我们不能直接相加安全强度。</p>
    <h3>一个更稳妥的难度关系</h3>
    <div class="formula">F(A) = F(B) 且 A ≠ B ⇒ SHA-256(A) = SHA-256(B)</div>
    <p>任何能高效找出共同碰撞的方法，也就能高效给出 SHA-256 的碰撞；对其他分量同样如此。因此，<strong>只要其中一个分量仍具备抗碰撞性，共同碰撞就不能被高效找到</strong>。这是条件性的归约关系，不是对真实哈希的无条件时间下界证明。</p>
    <p><a href="https://csrc.nist.gov/projects/hash-functions">NIST 对抗碰撞性的定义</a>强调的正是找到不同输入、相同输出在计算上不可行，而非碰撞不存在。本文的现实难度判断依赖这种密码学假设；计数证明本身不能证明“几乎不可能找到”。</p>
  </section>

  <section id="boundaries">
    <h2>原图的 PDF 要求，比这个命题更强</h2>
    <p>自由选择 A、B，寻找任意共同碰撞，是一个存在量词命题。给定文件 A，再找 B ≠ A 且 F(B) = F(A)，则是<strong>第二原像问题</strong>。鸽巢原理保证某个输出组里挤着多个输入，却不保证每个输出组都有多个输入。</p>
    <p>例如，把四个输入映射到三个结果，完全可以出现分组大小 2、1、1。碰撞已经存在，但落在单元素组里的输入没有第二原像。这说明，单凭计数，不能推出某个指定文件一定有对应样本。</p>
    <p>“合法 PDF”“显示特定内容”“只准修改某些位置”又增加了输入约束。全部 233 字节串并不都是合法 PDF，更不能保证碰撞的一组分别呈现想要的内容。要讨论受限集合，必须重新证明它的大小及性质；不能把任意字节串的结论直接移植过去。</p>
    <aside class="callout">
      <strong>这次讨论留下的准确结论</strong>
      <p>七项共同碰撞的原始字节串必然存在，甚至在固定 233 字节的集合内就能严格证明。寻找样本在现实计算资源下的可行性是另一个问题；它不由鸽巢原理决定，也不能用这份证明保证某份指定 PDF 可以按要求修改。</p>
    </aside>
  </section>

  <section id="plain-language">
    <h2>说人话：柜子总会装重，但你未必找得到</h2>
    <p>把每个文件想成一张不同的卡片。七种算法就像七台机器，每台读完卡片后，都按自己的固定规则给它打一个编号。我们把七个编号完整写在一张标签上：只有七项分别都相同，两张标签才算相同。</p>
    <p>再想象一个大仓库：每一种可能的标签对应一个柜子，卡片必须放进标签对应的柜子。虽然柜子多得难以想象，但编号的长度固定，所以柜子的数量有上限。</p>
    <p>这时只要卡片比柜子多，就一定有柜子装了不止一张卡片。就像把 11 张卡片放进 10 个柜子，不管怎么放，都不可能每个柜子最多只有一张。<strong>同一个柜子里的两张不同卡片，就是七项同时碰撞的两个文件。</strong>前面的计算还告诉我们：把全部 233 字节的可能输入都算上，卡片数至少是柜子数上限的 256 倍，所以至少有一个柜子能装下 256 张不同卡片。</p>
    <figure class="analogy-figure">
      <Image src={cardsAndCabinets} alt="两排共 10 个柜子，放入 A 到 K 共 11 张不同卡片。第 5 个柜子同时放着 E 和 F，其余各放一张。卡片代表不同文件，柜子编号代表七项结果的组合，同柜表示共同碰撞。" widths={[600, 1200, 1536]} sizes="(max-width: 820px) calc(100vw - 2rem), 760px" format="webp" loading="lazy" />
      <figcaption>11 张卡片放进 10 个柜子的一种分配：E 和 F 同柜，代表两个不同文件的七项结果分别相同。数字仅用于类比，真实输出组合远不止 10 种。</figcaption>
    </figure>
    <p>你可能会担心：“为了让第二个编号一样，改了卡片，不就把第一个编号弄变了吗？”但这里根本没有改卡片，只是在给已经存在的卡片分类。我们证明的是一定有卡片会被分到同一个柜子，并没有找到一种能随意修改编号的方法。</p>
    <p><strong>知道柜子里一定有重复，和找出哪两张卡片重复，是两件事。</strong>我们只看数量，就能断定重复存在，却没有因此拿到那两张卡片。如果没有捷径，逐张计算和比较的工作量可能大到现实中根本做不完。不过，这个比喻和计数证明都不能排除捷径；“很难找到”还需要对算法安全性的判断。它也不是说永远找不到：理论上，把有限的候选查到足够多，就一定能找到。</p>
    <p>最后，仓库里某个柜子装了多张卡片，不代表<strong>你手里这张卡片</strong>所在的柜子也有第二张。更不能保证第二张恰好是一份能正常打开、内容又符合要求的 PDF。</p>
    <p><strong>所以，数学能保证“某处一定有”，却没有替我们回答“具体在哪儿”“怎样找到”，更没有保证“你指定的这份文件就能改”。</strong>这正是那张梗图背后最有意思的区别。</p>
  </section>
  <section id="sources">
    <h2>参考资料</h2>
    <p>整理与核验日期：2026 年 9 月 20 日。本文的有限集合证明在正文中完整给出；算法位数、术语与结构性攻击的背景参考以下一手资料。</p>
    <ul>
      <li><a href="https://www.rfc-editor.org/rfc/rfc1321">RFC 1321：MD5</a>。本文仅引用输出长度，不沿用旧文中的安全性猜测。</li>
      <li><a href="https://www.rfc-editor.org/rfc/rfc1952">RFC 1952：GZIP 中使用的 CRC32</a>。CRC 变体需固定，32 位计数不依赖具体变体。</li>
      <li><a href="https://csrc.nist.gov/pubs/fips/180-4/upd1/final">FIPS 180-4：SHA-1 与 SHA-2</a>。</li>
      <li><a href="https://csrc.nist.gov/pubs/fips/202/final">FIPS 202：SHA-3</a>。</li>
      <li><a href="https://csrc.nist.gov/projects/hash-functions">NIST：哈希函数与抗碰撞、原像及第二原像性质</a>。</li>
      <li><a href="https://www.iacr.org/archive/crypto2004/31520306/multicollisions.pdf">Antoine Joux，Multicollisions in Iterated Hash Functions，CRYPTO 2004</a>。</li>
    </ul>
  </section>
</ArticleLayout>

<style>
  .analogy-figure { margin: 2rem 0; }
  .analogy-figure img { display: block; width: 100%; height: auto; border-radius: 8px; }
  .analogy-figure figcaption { margin-top: .8rem; line-height: 1.7; }
  .meme-figure { margin: 2rem 0; }
  .meme-figure a { display: block; width: fit-content; max-width: 100%; margin-inline: auto; }
  .meme-figure img { display: block; width: 100%; max-width: 560px; height: auto; border: 1px solid var(--line); border-radius: 8px; }
  .meme-figure figcaption { max-width: 560px; margin: .8rem auto 0; line-height: 1.7; }
  .formula { margin: 1.5rem 0; padding: 1rem 1.2rem; border: 1px solid var(--line); background: var(--surface); font-family: ui-monospace, "PingFang SC", monospace; font-size: .95rem; line-height: 1.9; overflow-wrap: anywhere; }
  .count-diagram { margin: 2rem 0; padding: 1.5rem; display: grid; grid-template-columns: 1fr auto 1fr; gap: 1rem; align-items: center; border: 1px solid var(--line); background: var(--surface); text-align: center; font-family: system-ui, sans-serif; }
  .count-diagram > div { display: grid; gap: .5rem; }
  .count-diagram strong { font-size: 1.35rem; color: var(--blue); }
  .count-diagram span { font-size: .85rem; }
  .count-diagram .diagram-arrow { font-size: 1.6rem; }
  figcaption { grid-column: 1 / -1; font-size: .8rem; color: var(--ink-soft); }
  .table-scroll { overflow-x: auto; margin: 2rem 0; }
  .table-scroll:focus-visible { outline: 3px solid var(--blue); outline-offset: 3px; }
  table { width: 100%; border-collapse: collapse; font-family: system-ui, sans-serif; font-size: .86rem; text-align: left; }
  caption { text-align: left; margin-bottom: .8rem; color: var(--ink-soft); }
  th, td { padding: .7rem .65rem; border-bottom: 1px solid var(--line); }
  thead { background: var(--blue-light); }
  @media (max-width: 520px) {
    .count-diagram { grid-template-columns: 1fr; }
    .count-diagram .diagram-arrow { transform: rotate(90deg); }
    .formula { padding: .8rem; font-size: .85rem; }
    th, td { padding: .6rem .4rem; }
  }
</style>
