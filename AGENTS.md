# AGENTS.md

## 项目定位

这是 Yann 的公开第二大脑，用来沉淀学习笔记、知识整理、实践经验，以及与 AI 深入讨论后的结论。

仓库内容会以网站形式公开发布，并持续部署到 Cloudflare。所有进入仓库的内容都应默认视为任何人都能长期访问、复制、搜索和引用。

## 核心原则

1. 只保存适合公开传播的知识，不保存个人秘密、内部资料或受限内容。
2. 在提交前主动检查并移除敏感信息，不依赖网站隐藏或仓库私有状态提供保护。
3. 将对话和零散材料整理成独立、清晰、可复用的知识，而不是直接堆放聊天记录。
4. 准确表达事实、推断和个人观点，并尽可能标明可靠来源。
5. 优先保持内容易读、结构简单、链接稳定，方便长期维护和回顾。

## 敏感信息与隐私

任何 Agent 在新增、修改、提交或发布内容前，都必须检查以下信息：

- API Key、Token、密码、Cookie、私钥、助记词和其他凭证。
- 真实住址、电话号码、身份证件、精确位置等个人身份信息。
- 私人邮箱、非公开账号、内部链接和带签名参数的 URL。
- 客户、同事、朋友或其他第三方的非公开信息。
- 公司内部代码、文档、会议内容、业务数据和未公开计划。
- 本地绝对路径、用户名、主机名、IP 地址、日志和环境变量中可能暴露身份或系统的信息。
- 付费内容、受版权保护的大段原文，以及许可证不允许公开的材料。

发现敏感信息时，应删除、匿名化或替换为明显的示例值，例如 `YOUR_API_KEY`、`user@example.com` 和 `example.com`。如果无法判断某项信息是否适合公开，先不要写入仓库，并向 Yann 说明风险。

不要仅在最新版本中删除已经提交的秘密。如果秘密曾进入 Git 历史，应立即提醒 Yann 撤销或轮换凭证，并在获得明确指示后清理历史记录。

## 内容整理方式

每篇知识文章应尽量做到：

- 聚焦一个清晰主题，标题能够准确说明内容。
- 开头简要说明问题、背景或学习目标。
- 给出经过整理的解释、结论、例子和适用边界。
- 区分已验证事实、合理推断、个人理解和仍待确认的问题。
- 引用外部资料时提供来源链接，并优先使用官方文档、原始论文或一手资料。
- 对可能随时间变化的信息注明日期或适用版本。
- 避免无上下文的聊天转录、重复内容和只有作者自己能理解的缩写。
- 使用简洁自然的中文；必要时保留准确的英文术语，并在首次出现时解释。

从 AI 对话沉淀内容时，应重新组织和核验信息，删除寒暄、提示词、重复推理和未经验证的断言。除非对理解主题确有帮助，否则不要保留原始对话形式。

## 内容实现方式

- 知识内容使用 Astro 页面和语义化 HTML 编写，不使用 Markdown 作为内容来源。
- 每篇内容放在 `src/pages/notes/` 下，并使用 `ArticleLayout.astro` 共享页面结构。
- 新增文章时，同时在 `src/data/articles.ts` 登记标题、摘要、发布日期、标签和公开路径。
- 需要解释关系、流程或结构时，优先使用清晰的 HTML、CSS、SVG 或专用图表组件，而不是用纯文字勉强描述。
- 图示必须提供可访问的文字说明，并在移动端保持可读；装饰元素不能替代正文信息。
- 只为当前内容引入必要的交互和依赖。能够在构建时生成的内容，不增加浏览器运行时 JavaScript。

## 文件与结构

- 文件名使用小写英文和连字符，例如 `how-transformers-work.astro`。
- HTML 使用语义化元素，标题层级连续且清晰。
- 图片和附件使用有意义的名称，放在与内容结构对应的资源目录中。
- 内部链接优先使用相对路径，避免绑定本地目录或临时地址。
- 移动或重命名已发布页面时，应考虑旧链接兼容和重定向。
- 未经必要性验证，不引入大型二进制文件、生成产物或无关依赖。

随着网站技术栈和目录结构确定，应遵循仓库中更具体的局部说明；若局部说明与本文件冲突，以保护隐私和公开发布安全为最高优先级。

## 网站开发与发布

- 网站使用 Astro 静态构建，输出目录为 `dist/`，面向 Cloudflare Pages 托管。
- 仓库只维护一套生产配置，以 `main` 为唯一发布分支，不维护 staging 或 preview 环境配置。
- 页面内容应兼顾桌面端与移动端，保持良好的可读性和基本无障碍体验。
- 不在客户端代码、构建产物或公开环境变量中放置秘密。
- 新增第三方脚本、统计服务、嵌入内容或外部资源前，应评估隐私、性能和长期可用性。
- 修改构建配置、路由或部署设置后，应运行相应检查并确认内部链接和主要页面可访问。
- 不提交本地缓存、依赖目录、临时文件、编辑器状态或包含个人信息的构建日志。

## Agent 工作规范

开始修改前，先阅读相关文件和已有约定，避免覆盖 Yann 的改动。只修改当前任务需要的内容，不顺带重构无关部分。

完成修改后：

1. 检查差异中是否含有敏感信息或不适合公开的内容。
2. 运行与改动相称的格式、构建或链接检查。
3. 清楚说明修改内容、验证结果以及仍存在的风险。
4. 未经 Yann 明确要求，不擅自发布网站、修改 Cloudflare 配置或执行破坏性 Git 操作。

提交信息应简洁说明实际变化。不要在提交信息、分支名、PR 描述或自动生成的元数据中加入提示词、秘密、个人路径或无关的 Agent 标记。
